<h1>Questions</h1>

  <% @questions.each do |question| %>
        <p><%= link_to question.title, question_path(question) %></p>
        <p><%= link_to question.body, question_path(question) %></p>
  <% end %>

<%= link_to 'Create new Question', new_question_path %>